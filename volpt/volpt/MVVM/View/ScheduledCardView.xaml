<UserControl x:Class="volpt.MVVM.View.ScheduledCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:volpt.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="320" d:DesignWidth="298.67">
    <Border CornerRadius="16" Background="#FFF9FAFB">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="26" />
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="#FF567DB8" CornerRadius="10,10,0,0">
                <Grid x:Name="header" Grid.Row="0">
                    <TextBlock x:Name="day" Text="Понедельник" 
                               Foreground="#FFE7E7EB" FontSize="18" 
                               HorizontalAlignment="Left" VerticalAlignment="Center"
                               Padding="10,2.5,10,2.5" />
                    <TextBlock x:Name="date" Text="29.10" 
                               Foreground="#FFE7E7EB" FontSize="18" 
                               HorizontalAlignment="Right" VerticalAlignment="Center"
                               Padding="10,2.5,10,2.5" />
                </Grid>
            </Border>
            <StackPanel Grid.Row="1"
                        Margin="8">

                <!-- ОДНА ПАРА -->
                <local:LessonsView/>
                <local:LessonsView/>
                <local:LessonsView/>
                <local:LessonsView/>

                <!-- ВТОРАЯ ПАРА -->
                <local:LessonsView />

                <!-- ТРЕТЬЯ ПАРА -->
                <local:LessonsView />

                <!-- Добавляй сколько нужно -->
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
